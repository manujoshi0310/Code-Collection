#include<bits/stdc++.h>

using namespace std;

int main(){
    vector<int> vec;
    vec.push_back(6);
    string str1 = "manu",str;
    int n=15;
    sort(str1.begin(), str1.end());

    cout <<floor(sqrt(n));
}